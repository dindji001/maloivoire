import 'package:flutter/material.dart';

class ListCommandePage extends StatelessWidget {
  const ListCommandePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Commande page"),
      ),
    );
  }
}
